apiVersion: v1
kind: Service
metadata:
  name: edc
  labels:
    app.kubernetes.io/name: edc
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: {{ .Values.container.port }}
      protocol: TCP
  selector:
    app.kubernetes.io/name: edc